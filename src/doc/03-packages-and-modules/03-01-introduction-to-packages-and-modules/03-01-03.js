/*
 * Packages and Modules
 *     Introduction to Packages and Modules
 *         About Public Packages
 *         About Private Packages
 */

console.log("\n-------------------------------------------------- 01");
{
  /*
   * public package 分为两种：
   *     unscoped public package
   *     scoped public package
   *
   * scoped public package 又分为两种：
   *     user-scoped public package
   *     org-scoped public package
   *
   *
   * unscoped public package 的包名形式为：xxx
   * scoped public package 的包名形式为：@user/xxx，@org/xxx
   */
}

console.log("\n-------------------------------------------------- 02");
{
  /*
   * private package 必须是 scoped private package，
   * 包括 user-scoped private package 和 org-scoped private package
   */
}
