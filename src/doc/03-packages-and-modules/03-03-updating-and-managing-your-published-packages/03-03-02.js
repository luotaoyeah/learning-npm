/*
 * Packages and Modules
 *     Updating and Managing Your Published Packages
 *         Unpublishing Packages from The Registry
 */

console.log("\n-------------------------------------------------- 01");
{
  /*
   * 当包发布到仓库之后，在 72 小时之内，可以取消发布，
   * 可以取消发布整个包，也可以取消发布包的某个版本，
   *     如果取消发布了整个包，则之后不能再重新发布这个包，除非重新换个包名，
   *     如果取消发布了某个版本，则之后不能再重新发布这个版本，必须更新版本，
   * 使用下面的命令：
   *     $ npm unpublish <package>@<version>
   */
}
